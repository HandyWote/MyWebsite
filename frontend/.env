# Local Next server and API rewrite target.
BACKEND_INTERNAL_URL=http://localhost:5000

# Canonical, Open Graph, JSON-LD, robots, sitemap and post-deploy prewarming origin.
PUBLIC_SITE_URL=http://localhost:3000

# Shared only by the backend worker and the Docker-internal Next route.
# Local dev: leave empty to keep on-demand revalidation disabled.
REVALIDATION_TOKEN=
