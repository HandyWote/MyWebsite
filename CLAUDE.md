# CLAUDE.md

根目录协作说明已重新初始化到 `AGENTS.md`。后续请以 `AGENTS.md` 为准，避免维护两份重复且容易过期的 agent 文档。

保留的顶层偏好：

- 回复请使用中文。
- 使用项目中已有组件，除非必要，不要另起炉灶。
- 不要在没有用户明确指令时修改业务代码。
- 如环境提供 `superpower` skill，必须使用；不可用时说明情况。
- 采用 TDD / REG 开发范式。
- 探讨方案时提供多个方案，并推荐未来技术债最低的方案。
- DONT USE CO-AUTHORED-BY WHEN COMMIT
